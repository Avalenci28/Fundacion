# TODO_FAVICON_TRANSPARENCIA (cancelado)

- [ ] Crear/exportar un favicon transparente en PNG (ej: `favicon-32x32.png`) y ponerlo en `frontend/public/`.
- [ ] Reemplazar referencias a favicon en `frontend/src/app/layout.tsx` (ya se dejó apuntando a `/favicon-32x32.png`).
- [ ] Agregar un hard refresh y limpiar caché de `Cached images and files`.
- [ ] Verificar en `http://TU_HOST:TU_PUERTO/favicon-32x32.png` que responde 200.
- [ ] (Opcional) Agregar `apple-touch-icon` y `manifest` si se necesita soporte móvil completo.

