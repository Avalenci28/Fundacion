# TODO_FIX_FAVICON_TITLE

- [x] Cambiar `metadata.title` en `frontend/src/app/layout.tsx` a `Malambo Sonríe`.
- [x] Cambiar `metadata.icons.icon` a `/favicon-32x32.png`.
- [ ] Verificar en navegador.
- [ ] Corregir compilación fallida (bloquea el despliegue y que se apliquen cambios).
  - [ ] En `frontend/src/components/admin/AdminProjectsCRUD.tsx` tipar `error` correctamente para evitar `error?.response`.

